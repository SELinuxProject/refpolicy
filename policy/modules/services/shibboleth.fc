/etc/shibboleth(/.*)?		gen_system_context(shibboleth_etc_t)

/usr/bin/shibd		--		gen_system_context(shibboleth_exec_t)

/usr/sbin/shibd		--		gen_system_context(shibboleth_exec_t)

/var/log/shibboleth(/.*)?	gen_system_context(shibboleth_log_t)
/run/shibboleth(/.*)?	gen_system_context(shibboleth_runtime_t)
